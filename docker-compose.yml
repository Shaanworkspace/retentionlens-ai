services:
  backend:
    build: ./backend
    ports: ["8000:8000"]
    environment:
      - SECRET_KEY=prod-secret-change-me
    volumes:
      - ./ml/models:/app/ml/models:ro
  frontend:
    build: ./frontend
    ports: ["5173:80"]
    depends_on: [backend]
